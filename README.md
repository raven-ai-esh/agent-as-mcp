# raven-skills

Библиотека для создания адаптивных AI-агентов с системой навыков. Агент автоматически подбирает подходящий навык для решения задачи, а при отсутствии такового — генерирует новый навык на основе диалога с пользователем.

В отличие от стандартных RAG или агентных систем, `raven-skills` реализует **обучение через опыт**: агент запоминает успешные решения и превращает их в переиспользуемые навыки.

## Ключевые возможности

- 🎯 **Автоматический подбор навыков** — поиск наиболее подходящего навыка через эмбеддинг-сходство
- 🔧 **Генерация навыков** — создание новых навыков из успешных диалогов
- 🧠 **Встроенный LLM-клиент** — вся логика промптов и SGR (Schema-Guided Reasoning) уже внутри
- 🔄 **Оптимизация** — автоматическое слияние похожих навыков
- ⚡ **Адаптивное улучшение** — три стратегии исправления при неудачном выполнении

## Установка

```bash
pip install raven-skills
```

## Быстрый старт

Библиотека теперь максимально проста в использовании. Вам нужен только клиент OpenAI и хранилище навыков.

```python
import asyncio
from openai import AsyncOpenAI
from raven_skills import SkillAgent, InMemoryStorage

async def main():
    # 1. Инициализация
    # Используем встроенное in-memory хранилище (или реализуйте своё через SkillStorage)
    agent = SkillAgent(
        client=AsyncOpenAI(),  # Ваш OpenAI клиент
        storage=InMemoryStorage(),
        llm_model="gpt-4o-mini",  # Модель для всех операций
    )

    # 2. Решаем задачу
    # Агент сам создаст эмбеддинг и найдёт навык
    task, result = await agent.match("Как задеплоить приложение?")

    if result.found:
        print(f"Найден навык: {result.skill.name}")
        
        # 3. Выполнение навыка
        # Агент выполнит шаги навыка, используя LLM
        execution = await agent.execute(result.skill, task)
        print(f"Результат: {execution.output}")
        
    else:
        print("Навык не найден, переходим в режим диалога...")
        # Здесь вы сами общаетесь с пользователем (свободный режим)
        # ...
        # Представим, что диалог прошёл успешно:
        conversation_log = [
            {"role": "user", "content": "Как задеплоить?"},
            {"role": "assistant", "content": "Сначала соберите образ..."},
        ]
        
        # 4. Сохраняем опыт в новый навык
        new_skill = await agent.generate_skill(
            task=task,
            conversation=conversation_log,
            final_result="Приложение задеплоено",
        )
        print(f"Создан новый навык: {new_skill.name}")

if __name__ == "__main__":
    asyncio.run(main())
```

## Как это работает?

`SkillAgent` — это единая точка входа. Внутри он инкапсулирует всю сложность работы с LLM:

1.  **Prompts & Templates**: Все системные промпты уже написаны и оптимизированы.
2.  **Schema-Guided Reasoning (SGR)**: Агент не просто генерирует текст, а заполняет строгие Pydantic-схемы (здесь и сейчас, внутри библиотеки). Это гарантирует, что шаги навыка будут всегда в правильном формате, а диагностика ошибок — структурированной.
3.  **Embeddings**: Генерация векторов для поиска происходит автоматически через переданный OpenAI клиент.

Вам больше не нужно писать свои промпты или парсеры.

## Хранение навыков

Для хранения навыков используется интерфейс `SkillStorage`. В комплекте идёт `InMemoryStorage` для тестов, но для продакшена вы легко можете реализовать свой (например, на Postgres + pgvector).

### Реализация своего хранилища

```python
from raven_skills import SkillStorage, Skill

class PostgresStorage(SkillStorage):
    def __init__(self, db_pool):
        self.db = db_pool

    async def save(self, skill: Skill) -> None:
        # Ваш SQL INSERT ...
        pass

    async def get(self, skill_id: str) -> Skill | None:
        # Ваш SQL SELECT ...
        pass
        
    async def get_all(self) -> list[Skill]:
        # Получить все навыки
        pass

    async def delete(self, skill_id: str) -> None:
        # Удаление
        pass

    async def search_by_embedding(
        self, embedding: list[float], top_k: int = 5, min_score: float = 0.0
    ) -> list[tuple[Skill, float]]:
        # Поиск ближайших соседей (vector search)
        pass
```

## Продвинутые возможности

### Диагностика и улучшение (Self-Correction)

Если навык сработал плохо, агент может диагностировать проблему и предложить исправление.

```python
# Представим, что выполнение прошло с ошибкой или пользователь недоволен
execution = await agent.execute(skill, task)

# Диагностика
action = await agent.diagnose(
    skill=skill,
    task=task,
    result=execution,
    user_feedback="Шаг 2 выдал ошибку авторизации",
)

print(f"Диагноз: {action.diagnosis}")
# Например: "wrong_steps" -> "Ошибка в шагах навыка"

# Исправление (создаёт новую версию навыка)
refined_skill = await agent.refine(skill, action)
```

### Слияние и оптимизация

Со временем у вас могут появиться дублирующиеся навыки. Агент умеет их находить и объединять.

```python
# Найти похожие навыки и автоматически объединить их
# (dry_run=False для реального применения изменений)
results = await agent.optimize(similarity_threshold=0.95, dry_run=False)

for originals, merged in results:
    print(f"Объединил {len(originals)} навыков в '{merged.name}'")
```

## Модели данных

### Skill

```python
@dataclass
class Skill:
    id: str
    name: str                  # Название
    metadata: SkillMetadata    # description, goal, keywords, embedding
    steps: list[SkillStep]     # Шаги выполнения
    version: int               # Версия навыка
    parent_id: str | None      # ID родителя (если это форк)
```

### Task

```python
@dataclass
class Task:
    id: str
    query: str                # Оригинальный запрос пользователя
    key_aspects: list[str]    # Выделенные LLM ключевые аспекты
    embedding: list[float]    # Вектор запроса
```

## Лицензия

Apache 2.0